def wordLadder(word1,word2):
	print "hello there"

	if word1 == word2:
		print "the words cannot be the same"
	if len(word1) != len(word2):
		print "the words must be the same length"



wordLadder("code","data")

wordLadder("code","datas")